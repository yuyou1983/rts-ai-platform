# Fixture Specification

Add `harness/evolve/runner_config.py` with `DEFAULT_TIMEOUT_SECONDS = 30` and a
unit test proving that the default timeout is 30 seconds.

The review must evaluate architecture compliance independently from whether the
requested timeout behavior was implemented.
