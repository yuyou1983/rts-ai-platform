# Fixture Specification

Add `simcore/policy_adapter.py` with a `select_policy()` function that imports
`StrategyAgent` from `agents.strategy` and returns its selected policy.

The review must evaluate literal specification compliance independently from
the repository architecture rules.
